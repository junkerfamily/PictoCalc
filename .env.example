# Copy to .env and change the password before sharing the machine
ADMIN_USER=admin
ADMIN_PASSWORD=changeme
PORT=8000
# Optional: persistent data directory (Railway volume mounts at /data)
# DATA_DIR=/data
